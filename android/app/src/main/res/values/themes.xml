<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      No AppCompat dependency: the host uses a platform Material theme. The
      WebView fills the window; IME inset handling lives in code (todo 17).
    -->
    <style name="Theme.Simplify2Md" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
